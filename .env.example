# TrustRAG — 환경변수 목록
# 이 파일은 GitHub에 커밋됩니다 (실제 값 없음)
# 실제 값은 .env 파일에 (gitignore 처리)

# ── Supabase (TrustRAG 전용 새 프로젝트) ──
SUPABASE_PROJECT_ID=
SUPABASE_URL=https://{PROJECT_ID}.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=
SUPABASE_TOKEN=          # Management API 토큰 (SQL 실행용)

# ── OpenAI ──
OPENAI_API_KEY=

# ── n8n ──
N8N_API_KEY=
N8N_BASE_URL=https://jknetworks.app.n8n.cloud

# ── Google Drive ──
# n8n 내부 Google 인증 사용 (별도 키 불필요)
DRIVE_ROOT_FOLDER=TrustRAG   # Google Drive 루트 폴더명

# ── GitHub ──
GITHUB_TOKEN=

# ── Netlify (office-ai.app 배포) ──
NETLIFY_TOKEN=
NETLIFY_SITE_ID=7b9d2cad-0a57-4d30-bbdf-54f80848ce94
